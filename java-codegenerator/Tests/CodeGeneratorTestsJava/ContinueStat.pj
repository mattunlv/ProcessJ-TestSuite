/**
 * ContinueStat program!
 */
proc void main(){
  int x = 0;

  while(true){
    x++;
    if( x % 2 == 0)
      continue;
  }
  
  //Java will error here saying unreachable code.
  while(true)
    continue;

  do{
    continue;
  }while(true);

  return;
}
