/**
 * Simple program!
 */
proc void main(){
  int x = 0;

  if( x < 0)
    x++;

  if( x > 0)
    x--;
  else
    x = 100;

  if( x > 0){
    x--;
  }
  else{
    x = 100;
    int z = 20;
  }
  return;
}
